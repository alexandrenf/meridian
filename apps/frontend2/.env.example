# Database URL for connection
DATABASE_URL="postgres://username:password@localhost:5432/meridian"

# API endpoint for the worker (used in original for open graph images)
NEXT_PUBLIC_WORKER_API="https://worker-api.example.com" 